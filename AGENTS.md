# AGENTS.md

## Project overview

このリポジトリは、ポケモンカードのパック／BOX開封結果を再現する非公式の静的Webアプリです。

- フロントエンドは Vanilla HTML / CSS / JavaScript（ES Modules）で構成する。
- ビルド工程はなく、`index.html` がアプリの入口である。
- `src/app.js` はデータ読み込み、UI、結果表示、画像出力を担当する。
- `src/simulator.js` はパック／BOXの抽選、制約適用、検証、集計を担当する。
- `data/cards/` はカードマスタ、`data/rules/` はセット別封入ルール、`data/sets/index.json` は対応セット一覧である。
- `assets/cards/` と `assets/packs/` に表示用画像を置く。
- `tools/` のPythonスクリプトは公式ページ由来のデータ取得、画像取得、カードマスタ整備に使用する。
- 本アプリの封入率には、公式発表ではなく参考情報や実測、ユーザー仮説に基づく推定値が含まれる。

## Project principles

- 既存仕様とデータの関係を理解してから変更する。
- 推測だけで既存仕様、封入率、カードデータを変更しない。
- コード、データ、既存ドキュメント、テストの根拠を区別して説明する。
- 不明な仕様は、関連コード、ルールJSON、カードマスタ、既存ドキュメント、テストの順に確認する。
- コードとルールJSONが食い違う場合は、どちらかを無断で正とせず、現在の実挙動と意図された仕様を切り分ける。
- セット固有の違いは、可能な限り `data/rules/{setCode}.json` で表現し、共通ロジックへのハードコードを増やさない。
- 非公式ファンツールであることと、画像・名称等の権利関係に配慮する。

## Repository map

```text
index.html                 アプリ本体のHTML
disclaimer.html            免責事項・サイト情報
src/app.js                 UI、データ読込、表示、Canvas画像出力
src/simulator.js           抽選・制約・検証・集計ロジック
src/styles.css             共通スタイル
data/sets/index.json       対応セット一覧と各データへの参照
data/cards/{setCode}.json  セット別カードマスタ
data/rules/{setCode}.json  セット別封入ルール
data/raw/                  公式カード詳細ページのHTMLキャッシュ
assets/cards/{setCode}/    セット別カード画像
assets/packs/              パック画像
tools/                     データ取得・整備用Pythonスクリプト
```

## Development workflow

### 作業開始時

1. 依頼内容に関係するコード、JSON、画像参照を確認する。
2. `docs/dev/current-state.md` が存在する場合は確認する。
3. 関係する `docs/decisions/` の記録が存在する場合は確認する。
4. 関連テストが存在する場合は、変更前の期待値を確認する。
5. 中規模以上の変更では、実装前に簡潔な計画と影響範囲を提示する。

現時点では `docs/` と自動テスト基盤は存在しないが、必要に応じて新設してよい。中規模以上の仕様変更、抽選ロジック変更、長期的な設計判断を行う際は、作業範囲に応じてドキュメントやテスト基盤の追加も検討する。新設時は目的、配置、実行方法、更新責任を明確にし、READMEと本ファイルにも導線を追加する。

### 実装時

- 変更範囲を必要最小限にする。
- 既存設計、命名規則、日本語UIを尊重する。
- 関係のないリファクタリングや整形を同時に行わない。
- 新しい依存ライブラリを追加する場合は、必要性、代替案、配布方法への影響を説明する。
- ビルド工程のない静的アプリという性質を維持するか、変更する場合は事前に理由を示す。
- テストやドキュメントの不足が安全な実装・検証を妨げる場合は、依頼された変更に必要な範囲で基盤を追加してよい。
- ユーザーの許可なくカードマスタ、画像、HTMLキャッシュを一括再生成しない。
- 推定封入率やBOX構成を変更する場合は、変更根拠と期待される分布を明記する。
- 相対URLはJSONファイルの位置ではなく、表示中のHTML文書を基準に解決されることに注意する。
- HTML、CSS、JavaScript、JSON、Markdown、PythonはUTF-8を維持する。
- 日本語を含むファイル名や `imageLocalPath` を変更するときは、実ファイルとの対応を必ず保つ。

### 検証時

- 変更に直接関係するテストを実行する。
- lint、typecheck、構文チェックが利用可能な場合は実行する。
- テスト基盤がない領域では、再現手順と手動確認内容を明記する。
- テスト失敗や封入制約違反を無視して完了扱いにしない。
- 乱数ロジックの変更は、単発結果だけで判断せず、十分な回数のBOX生成による分布と不変条件を検証する。
- 実行できなかった検証は、その理由と残るリスクを報告する。

### 作業終了時

- 変更内容とユーザーへの影響を要約する。
- 実行したテスト、手動確認、結果を報告する。
- 未解決事項、既知の制約、推測を明記する。
- 状態が変化した場合は `docs/dev/current-state.md` を更新する。ファイルがまだなく、今後も参照価値がある変更なら新設してよい。
- 重要かつ長期的な設計判断を行った場合のみ `docs/decisions/` に記録する。初回はディレクトリと簡潔な運用説明を新設してよい。
- 再利用価値のある知見が得られた場合のみ `docs/dev/lessons.md` に追記する。継続的な価値がある場合は新設してよい。
- ドキュメントは単発の作業ログではなく、将来の判断や実装に利用できる内容にする。

## Local development

アプリはJSONを `fetch()` するため、原則として `file://` で直接開かず、リポジトリルートをHTTPサーバーで配信する。

```powershell
py -m http.server 8000
```

ブラウザで `http://localhost:8000/` を開く。

現時点では `package.json`、Node.js依存関係、標準化されたテスト／lintコマンドはない。機能追加や抽選ロジックの修正に自動検証が必要な場合は、過剰にならない範囲でテスト基盤を新設してよい。新設した場合は、このセクションとREADMEに、セットアップ、実行コマンド、対象範囲を追記する。

テスト基盤を選定するときは、次を優先する。

- ES Modulesを直接扱えること。
- `src/simulator.js` の純粋なロジックをブラウザなしで検証できること。
- 乱数を注入または固定して、失敗を再現できること。
- UIテストは重要なユーザーフローに絞り、抽選ロジックの単体テストと分離すること。
- 導入する依存関係と設定を必要最小限にすること。

## Data contracts

### Set index

`data/sets/index.json` の各セットは、少なくとも次を持つ。

```text
setCode
displayName
cardMasterPath
rulePath
thumbnailPath
```

新しいセットを追加するときは、カードマスタ、封入ルール、カード画像、パック画像、セット一覧の参照を一体として確認する。

### Card master

`data/cards/{setCode}.json` の主要フィールドは次のとおり。

```text
officialCardId
setCode
cardNo
cardNoTotal
name
rarity
category
pokemonType
trainerType
evolutionStage
hp
packName
sourceUrl
imageUrl
imageLocalPath
```

- `category` は主に `pokemon`、`trainer`、`energy` を使用する。
- 公式検索に存在しない高レアカードは、手動追加データの場合がある。
- `imageLocalPath` を優先し、なければ `imageUrl`、さらに失敗時はプレースホルダーを使用する。
- `officialCardId`、カード番号、レアリティ、カード名の組み合わせが識別処理に影響するため、変更時は集計と重複判定も確認する。
- `-L` / `-R` のカードは、公式IDが同じでも別カードとして扱う既存仕様がある。

### Rule data

`data/rules/{setCode}.json` は封入仕様を表す。ただし、現在は一部のキーと実装が完全には一致していない。

特に次の項目を変更するときは、JSONを読むコードパスを確認し、設定が実際に利用されていることをテストする。

```text
cardsPerPack
baseSlots
boxRules.boxRarityCounts
boxRules.boxHit
boxRules.fixedBoxInclusions
boxRules.boxCardCopyLimits
boxRules.trainerBoxRules
packRules.slotRules
```

設定を追加しただけで対応完了とせず、実際の抽選処理がその設定を参照していることを確認する。

## Simulation invariants

抽選ロジックを変更するときは、少なくとも次を維持または意図的に更新する。

- 現在の標準は1BOX 30パック、1パック5枚である。
- 15パック開封は、生成した1BOXをシャッフルして15パック抽出する。
- 1パック内で同一カードを重複させない。
- R、RR、AR、SR以上の当たりカードは原則として4枠目を共有し、同一パックに複数の当たり枠を作らない。
- C・UのBOX内コピー枚数制限を守る。
- トレーナーズの1パック上限と、セット別の出現可能スロットを守る。
- M6ではC・Uの通常トレーナーズをBOX単位で計画する。
- M6の伝説スタジアム `-L` / `-R` はペアとして扱う。
- 高レア2枚箱では、セット別ルールに従って追加枠の候補を制限する。
- ルール生成に失敗して最終試行結果を返す経路があるため、警告の有無も検証対象にする。

乱数の再現性が必要なテストを追加する場合は、乱数生成器を注入可能にする設計を優先し、本番挙動を不必要に変えない。

抽選ロジックへ実質的な変更を加える場合は、可能な限り同じ変更内で次の自動テストを追加する。

- 固定乱数またはシードによる代表ケースの再現テスト。
- 1パックの枚数、重複禁止、トレーナーズ上限などの不変条件テスト。
- BOX単位のレアリティ枚数とコピー枚数制限のテスト。
- セット固有ルールの回帰テスト。
- 多数回生成による分布テストは、確率的に不安定にならない許容範囲と試行回数を設定する。

## Recommended verification checklist

変更内容に応じて以下を確認する。

- セット一覧と初期選択が正しい。
- カードマスタとルールJSONが読み込める。
- パック画像、カード画像、フォールバックが表示される。
- 15パック、30パックの両方を開封できる。
- 全パックが期待枚数を持つ。
- パック内に同一カードがない。
- レアリティ別封入枚数がルールに一致する。
- C・Uのカード別最小／最大枚数が制限内にある。
- トレーナーズ枚数、種別、スロット、1パック上限が正しい。
- M6のL/Rペアが成立する。
- カード別集計、フィルター、拡大表示が動作する。
- ハイレア一覧と全カード一覧のPNGを生成できる。
- デスクトップとモバイル幅でレイアウトが破綻しない。
- 読み込み失敗や画像欠損時に利用可能なエラー表示またはフォールバックがある。

## Data tools and external access

- `tools/fetch-card-master.py` と `tools/fetch_card_images.py` は外部サイトへアクセスし、ローカルデータを更新し得る。
- 実行前に対象セット、カードID範囲、出力先、キャッシュ利用、上書きオプションを確認する。
- 大量取得時は `--sleep` を設定し、公式サイトへ過剰な負荷をかけない。
- 可能な限り `data/raw/` の既存HTMLキャッシュを利用する。
- 外部HTML構造に依存するため、パーサー変更時は保存済みHTMLで複数カテゴリのカードを確認する。
- JSONを更新するツールを実行する前に、変更対象と差分が意図どおりか確認する。
- 画像や取得データの公開・再利用について、利用条件、著作権、商標、権利者からの要請に配慮する。
- Python依存関係は現在固定されていない。依存管理を導入した場合はREADMEと本ファイルを更新する。

## Known design risks

- `src/simulator.js` は大きな単一モジュールで、抽選、補正、検証、集計の責務が集中している。
- ルールJSONの `cardsPerPack`、`baseSlots`、`boxRules.boxRarityCounts` と、現在の参照コードに乖離がある。
- BOX生成が制約を満たせない場合、警告を出して最後の生成結果を返す。
- `Math.random()` を直接使用しており、再現可能なテストが難しい。
- セット切替の非同期読み込みにはキャンセルや世代管理がなく、連続操作時に競合する可能性がある。
- `html2canvas` はCDNから読み込まれているが、現在の画像出力はCanvas APIを直接使用している。
- 自動テスト、lint、typecheck、CI、依存バージョン固定は現時点で存在しない。
- READMEは現在の対応セットと構成を十分に反映していない。

既知の問題を別作業のついでに無断で修正しない。依頼範囲に含まれる場合は、現状、期待仕様、変更の影響を明確にして対応する。

## Safety

以下はユーザーの明示的な指示なしに実行しない。

- productionへのdeploy
- secretやAPI keyの追加・変更・削除
- production databaseや外部データストアの変更
- データ、カード画像、HTMLキャッシュの一括削除
- 外部サイトからの大量取得または一括再取得
- force push
- mainブランチへの直接push
- 履歴を書き換えるGit操作

破壊的操作の前には対象を明示し、可能ならバックアップまたは復元手段を確保する。
